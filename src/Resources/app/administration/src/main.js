// Entry point auto-discovered and bundled by the Shopware Administration build.
// Importing the component is enough to register it for use via <component
// name="asbs-streamdeck-api-keys"> in the plugin's config.xml.
import './component/asbs-streamdeck-api-keys';
